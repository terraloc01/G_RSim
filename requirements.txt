# G_RSim 의존성 — .venv (Python 3.12) 기준
# gprMax 는 PyPI 미배포: external/gprMax 소스에서 빌드 설치
#   git clone https://github.com/gprMax/gprMax.git external/gprMax
#   pip install setuptools wheel
#   pip install external/gprMax --no-deps --no-build-isolation  (MSVC Build Tools 필요)
numpy==1.26.4
cython<3.1
h5py
scipy
matplotlib
psutil
colorama
terminaltables
tqdm
# PySide6 6.11.1 은 shiboken 훅이 six.moves import 시 크래시 (matplotlib qtagg 경유) -> 6.10.1 고정
PySide6==6.10.1
